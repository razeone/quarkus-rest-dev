package com.ibm;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends CardTransactionEndpointTest {

    // Execute the same tests but in native mode.
}