package com.ibm;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCardTransactionResourceIT extends CardTransactionEndpointTest {

    // Execute the same tests but in native mode.
}